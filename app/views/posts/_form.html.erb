<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :postImage %><br>
    <%= f.file_field :postImage %>
  </div>

   <%#=f.label :SelectInterest %>
    <%#= f.collection_select :interest_ids , Interest.order(:name) ,:id,:name,{},{multiple:true}  %>

    <div class="field">
      <%= f.label :interest_tokens, "Authors" %><br/>
      <%= f.text_field :interest_tokens %>
    </div>


    <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
